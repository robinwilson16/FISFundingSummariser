CREATE VIEW VW_FIS_Pathways
AS
	SELECT
		FrameworkCode = PWAY.CODE,
		ProgType = PWAY.PROG_TYPE,
		PathwayCode = PWAY.PATHWAY_CODE,
		PathwayName = PWAY.PATHWAY_NAME
	FROM [EBS-DB\EBS].ebs.dbo.APP_FRAMEWORK_PATHWAYS PWAY
	WHERE
		PWAY.IS_ACTIVE = 'Y'
		AND PWAY.FUNDING_YEAR = '31'